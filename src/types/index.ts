// Types pour media query - AUCUN ANY

export interface NavItem {
  id: string;
  label: string;
  icon: string;
  href: string;
}

export interface LayoutCard {
  id: string;
  title: string;
  description: string;
  image: string;
}